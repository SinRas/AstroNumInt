// Undone
double n2( double s, double sp, double delta_c, double delta_cp ){
	// Calculate and Return
	// \int^{\infty}_{0} v*vp* p( \delta_c, \delta_cp, v, vp ) * dv * d(vp)
	// where
	// 	p( \delta_c, \delta_cp, v, vp ) := p4( \delta_c, \delta_cp, v, vp )
	return( 1.0 );
}
